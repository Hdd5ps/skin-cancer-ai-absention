fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
# Base runtime dependencies only.
# For model inference on glibc environments (Ubuntu/Debian), also install:
#   pip install -r backend/requirements.glibc-ml.txt
# This keeps Alpine/musl setups installable for health-mode startup.
Pillow==12.3.0
numpy==2.5.2
pydantic==2.13.4
pytest==9.1.1
httpx==0.28.1
pytest-asyncio==1.4.0
python-dotenv==1.0.0
# Security dependencies
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9
